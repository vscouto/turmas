<%= form_for(@nota) do |f| %>
  <% if @nota.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nota.errors.count, "error") %> prohibited this nota from being saved:</h2>

      <ul>
      <% @nota.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :turma %><br />
    <%= f.collection_select(:turma_id, Turma.all, :id, :codigo) %>
  </div>
  <div class="field">
    <%= f.label :aluno %><br />
    <%= f.collection_select(:aluno_id, Aluno.all, :id, :nome) %>
  </div>
  <div class="field">
    <%= f.label :g1 %><br />
    <%= f.text_field :g1 %>
  </div>
  <div class="field">
    <%= f.label :g2 %><br />
    <%= f.text_field :g2 %>
  </div>
  <div class="field">
    <%= f.label :g3 %><br />
    <%= f.text_field :g3 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
